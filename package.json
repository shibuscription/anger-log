{
  "name": "angar-app",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "seed:test-user": "node scripts/seed-anger-logs.js --apply",
    "seed:test-user:dry": "node scripts/seed-anger-logs.js --dry-run",
    "cleanup:test-user": "node scripts/cleanup-seeded-anger-logs.js --apply",
    "cleanup:test-user:dry": "node scripts/cleanup-seeded-anger-logs.js --dry-run"
  },
  "dependencies": {
    "firebase": "^12.6.0",
    "firebase-admin": "^13.6.0"
  },
  "devDependencies": {
    "vite": "^7.1.8"
  }
}
